import React, { useEffect, useMemo, useState } from 'react';
import { api } from './api';
import LoadingButton from './LoadingButton';
import type { SettingsMap, ToastPush } from './types';
export default function SettingsPanel({ push }: { push: ToastPush }) { const [settings, setSettings] = useState<SettingsMap>({}); const [loading, setLoading] = useState(false); useEffect(() => { api('/api/settings').then((d) => setSettings(d.settings || {})).catch((e) => push('error', e.message)); }, []); const keys = useMemo<string[]>(() => Object.keys(settings).sort(), [settings]); return <section className="rounded-2xl border border-[var(--border)] bg-[var(--surface)] p-5"><h2 className="text-lg font-semibold">站点设置</h2><p className="mt-1 text-sm text-[var(--muted)]">保存后会刷新公开设置缓存。</p><div className="mt-5"><div className="grid gap-3 md:grid-cols-2">{keys.map((key) => <label key={key} className="block text-sm">{key}<input value={settings[key]} onChange={(e) => setSettings({ ...settings, [key]: e.target.value })} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-transparent px-3 py-2" /></label>)}</div><div className="mt-4"><LoadingButton loading={loading} onClick={async () => { try { setLoading(true); await api('/api/settings', { method: 'PUT', body: JSON.stringify({ settings }) }); push('success', '设置已保存'); } catch (e: any) { push('error', `设置保存失败: ${e.message}`); } finally { setLoading(false); } }} className="rounded-xl bg-[var(--primary)] px-4 py-2 text-sm font-medium text-white">保存设置</LoadingButton></div></div></section>; }